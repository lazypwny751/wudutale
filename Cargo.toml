[package]
name = "takfirtale"
version = "0.1.0"
edition = "2024"

[dependencies]
tetra = "0.8"
rand = "0.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

